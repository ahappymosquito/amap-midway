# 项目协作规范

- 提到库时，使用最新的稳定库版本进行代码编写，并在依赖文件中体现。
- 做项目时，将开发沟通中的规范添加到项目下的 `AGENTS.md`。
- 每次改动都同步更新项目文档，保持项目文档与当前代码状态一致。
- 每次执行完任务且测试通过后，如果当前目录是 git 仓库，需要创建 git 提交；如果不是 git 仓库，则不提交并说明原因。
- 开发完成后启动本地服务供用户测试，默认地址 http://127.0.0.1:8000/ ；若已有实例在跑则先复用，不要重复占端口。
- 用户提示词优先级最高；如果用户提示词与项目文档冲突，以用户提示词为准。
- 用户需要了解技术实现：每个代码文件开头必须表明该文件实现了什么功能；每次改动代码文件都要同步更新文件开头介绍。
- 本项目首版将“租房房源小区”定义为高德住宅小区 POI，不接入真实租金、户型或平台房源链接。
- 选址支持 1 到 6 个通勤点；打开页面用高德 JS 定位（GCJ-02，失败则 IP 粗定位）默认填入当前位置，一个点即可搜附近。地点可分行或用分号（；/;）批量导入。默认 300 元/人。餐馆按高德人均判断，酒店按高德人均/参考价判断，实时房价不在站内爬取。
- 排序可选通勤时间（最慢一侧地铁，可设 20/30/45/60 分钟上限）或绝对距离（直线距离，并可选 1.5/3/5/8km 搜索范围）；搜索时为前 3 名推荐预规划多种地铁方案和骑行方案。
- 左上角浮窗列出多种路线，地图同一时间只画一种出行方案（地铁或骑行），不要叠在一起。
- 搜索后只标注地铁站图标和站名；选中路线后额外标出途经站，不再画站与站之间的粗线。
- 城市由地点名自动推断，页面不再单独填写城市。
- 地点、人数、预算、餐馆/酒店选项记在浏览器 localStorage，下次打开自动带回；最近地点可点选填入。
- 预算文案随选项切换：餐馆显示人均预算，酒店显示人均差标。
- 选址品类对齐扫街榜2026状元榜：美食、酒店、玩乐、咖啡、酒吧、景点。美食保留状元榜/烟火小店/极致甄选；其他品类用状元榜关键词召回。搜美食/咖啡/酒吧后附带附近玩乐，搜玩乐/景点后附带附近美食。网页暂无咖啡/酒吧/玩乐独立榜时跳转城市扫街榜总览。
- 酒店提供温泉/电竞/独家/华住会/亲子/民宿属性（店名/标签关键词召回），以及大床房/双床房/家庭房/套房一级床型（按携程该店房型列表归类，不按店名里的「大床房」字样）；实时空房和房价仍跳转美团/携程查看。跳转 www.amap.com/ranking/{城市}/hotel 必住榜。
- 选中路线用描边加低频闪烁与底图区分；左侧方案合计前展示地铁/骑行或走路/换乘，合计后展示分段。
- 地铁方案末段默认是下地铁后骑行到店，400 米内或约 4 分钟走路才保留步行。
- 桌面端跳转美团（酒店 H5 / 餐馆到餐页）和携程（cityId + searchWord）网页；移动端高德可走 App 链接。
- 地图只使用高德 JS API 2.0，不引入 Leaflet、Mapbox 或其他地图库。 JS 规划失败时回退到后端 `/api/places/route`。
- 产品页不再提供附近小区搜索。
- 高德 Key 通过 `AMAP_WEB_KEY` 环境变量配置，代码中不得硬编码 Key。
- 前端采用 FastAPI 托管的原生 HTML/CSS/JS 单页，不引入 React/Vite 构建链。
